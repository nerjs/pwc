import React from 'react'
import styled from 'styled-components'

const BodyTabsContainer = styled.div``

const BodyTabs = ({ active }) => {
    return <BodyTabsContainer>{active}</BodyTabsContainer>
}

export default BodyTabs

import React, { useEffect } from 'react'

const ControllMap = ({ map, ...props }) => {
    // useEffect(() => {
    //     if (!map) return
    //     setTimeout(() => {
    //         map.panTo({ lat: 77, lng: 77 })
    //     }, 5000)
    // }, [map])

    return null
}

export default ControllMap
